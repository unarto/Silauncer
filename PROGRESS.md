PROGRESS.md update done
- **SELESAI**: Mengganti semua penggunaan SharedPreferences dengan MMKV untuk performa yang lebih baik.
  - Menambahkan dependensi `com.tencent:mmkv`.
  - Menginisialisasi `MMKV` di `LauncherApplication`.
  - Memperbarui `PreferenceStore`, `TileStore`, `BaseScriptTileService`, dan `QuickSettingsActivity` untuk menggunakan MMKV.
- **SELESAI**: Memperbarui versi dependensi MMKV ke versi stabil terbaru (1.3.17) untuk mengatasi pesan log warning terkait penggunaan ashmem (`Pinning is deprecated since Android Q`) pada Android versi baru.
- **SELESAI**: Refactor Storage MMKV secara modular:
  - Membuat `LauncherPreferences`, `TileConfigStorage`, dan `TileStateStorage` untuk memisahkan tanggung jawab.
  - Memindahkan model `TileConfig` ke package yang sesuai.
  - Mengimplementasikan migrasi data dari format lama ke MMKV ID yang baru.
  - Memastikan tidak ada akses MMKV secara langsung dari Activity atau Service.
- **SELESAI**: Menghapus seluruh implementasi fitur Quick Settings Tile dari proyek Silauncer:
  - Menghapus direktori `quicksettings` beserta isinya (`QuickSettingsActivity`, `BaseScriptTileService`, dan turunannya).
  - Menghapus eksekutor `RootCommandExecutor` dan `ShizukuCommandExecutor`.
  - Menghapus class penyimpanan `TileConfigStorage` dan `TileStateStorage`.
  - Menghapus dependensi `shizuku` di `build.gradle.kts`.
  - Menghapus referensi `QuickSettingsActivity` dan tombol pengaturannya di `SettingsActivity`.
  - Membersihkan deklarasi `Service` dan `Activity` yang terkait dengan fitur Quick Settings dari `AndroidManifest.xml`.
  - Menghapus *string resource* dan drawable ikon yang sudah tidak terpakai (seperti `ic_lightning`).
- **SELESAI**: Menambahkan panduan `/launcher.md` berisi instruksi untuk merebuild Android Launcher agar lebih ringan, cepat, minimalis, dan touch-first.
- **SELESAI**: Merevisi `AppInfo.kt` sesuai struktur yang diminta, dan memisahkan pemrosesan icon menggunakan `IconCache`. Memperbarui `AppLoader.kt`, `AppAdapter.kt`, dan `LauncherActivity.kt` untuk beradaptasi dengan model data yang baru.
- **SELESAI**: Mengimplementasikan `AppRepository` dan `AppSorter` berdasarkan contoh referensi, serta menghapus `AppLoader` untuk memisahkan logic pengambilan data aplikasi (via `LauncherApps`) dan sorting. Mengintegrasikan pembaruan ini ke dalam `LauncherActivity` dengan struktur coroutine yang aman untuk performa.
- **SELESAI**: Merombak (rebuild) App Loading Engine Silauncer mengikuti pola matang AOSP Launcher3.
  - Memisahkan komponen aplikasi: `AppInfo`, `AppRepository` (cache daftar aplikasi persisten dengan operasi IO di background), dan `IconCache` (menghindari duplikasi pengambilan ikon oleh `PackageManager`).
  - Mengimplementasikan `AppChangeReceiver` untuk mengamati perubahan aplikasi (install, remove, update) secara parsial/incremental dari sistem.
  - Mengonversi `AppAdapter` menggunakan `ListAdapter` dan `DiffUtil` demi performa dan pembaruan UI incremental, serta menyederhanakan ukuran ikon (di-*pass* dalam bentuk piksel, bukan dibaca dari *preferences* berulang kali).
  - Menyederhanakan `LauncherActivity` sehingga lebih cepat karena tidak melakukan scan penuh aplikasi saat `onResume()`. 
  - Menambahkan utilitas peredaman pantulan gulir (`OverScroll`) menggunakan `SpringAnimation` AndroidX Dynamic Animation agar pengalaman gulir terasa responsif namun ringan.
- **SELESAI**: Memperbarui MMKV ke versi terbaru `2.4.1` untuk memperbaiki peringatan `Pinning is deprecated since Android Q` pada Android terbaru yang muncul akibat masalah kompatibilitas ashmem internal MMKV versi sebelumnya.
- **SELESAI**: Melakukan refactor `LauncherActivity.kt` berdasarkan *Single Responsibility Principle (SRP)*:
  - Membuat `LauncherAppController` untuk orkestrasi *loading* dan *refreshing* data aplikasi dari `AppRepository` dan mengurutkannya menggunakan `AppSorter`.
  - Membuat `AppActionHandler` untuk menangani *action intent* (luncurkan aplikasi, tampilkan *App Info*, dan buka dialog *uninstall*).
  - Membersihkan `LauncherActivity` dari *business logic* sehingga hanya bertanggung jawab menangani *UI/RecyclerView wiring* dan *lifecycle*.
  - Mengonversi `CoroutineScope(Dispatchers.Main)` menjadi *lifecycle-aware coroutine* menggunakan `lifecycleScope`.
- **SELESAI**: Audit *Single Responsibility Principle (SRP)* pada `LauncherAppController` dan Concurrency pada `AppRepository`.
  - Menemukan *race condition* karena `AppRepository.loadInitialApps()` berjalan di `Dispatchers.IO` sementara operasi manipulasi *package* lainnya berjalan di `Dispatchers.Main` (via `AppChangeReceiver`).
  - Menambahkan `@Synchronized` pada fungsi manipulasi daftar aplikasi dalam `AppRepository` sebagai solusi aman tanpa merombak arsitektur Coroutine yang sudah sesuai batasan *Controller*.
  - Menghapus dependensi struktur instance `LauncherPreferences` utuh dari `AppSorter` yang sekarang murni menerima _string_ (`sortMode`).
- **SELESAI**: Eksekusi PHASE 1 (SRP Audit & Refactor):
  - Memisahkan tanggung jawab *Cache* dan *Icon Loading*.
  - `IconCache.kt` sekarang menjadi *pure cache storage* yang hanya mengurus fungsi _get, put, removePackage_, dan _clear_.
  - Membuat `IconLoader.kt` yang secara spesifik mengambil (loading) _Drawable_ icon dari `PackageManager` OS Android jika belum ada di *Cache*.
  - Mengubah referensi pada `AppAdapter` agar memanggil `IconLoader` tanpa mengubah sisa sistem _launcher behavior_ ataupun UI.
- **SELESAI**: Eksekusi PHASE 2 (SRP Audit & Refactor):
  - Memisahkan *business logic* dari `AppChangeReceiver`.
  - `AppChangeReceiver` kini murni berfungsi sebagai penerus *intent* dan tidak melakukan pembaruan `AppRepository` maupun `IconCache` secara mandiri.
  - Memindahkan logika respon perubahan paket (tambah, hapus, perbarui) beserta invaldasi _cache_ secara spesifik (incremental) ke dalam `LauncherAppController` (`handlePackageEvent`).
  - Tidak terjadi penambahan lapisan arsitektur *event/manager* berlebih (hanya mengoper via *higher-order function / callback* biasa ke UI yang lantas memanggil `AppController`).
- **SELESAI**: Mengubah `applicationId` dari `com.silauncer.fihsfp` menjadi `com.silauncer.cepat` pada `app/build.gradle.kts`.
- **SELESAI**: Melakukan refactor pada `SettingsActivity` dengan menerapkan Single Responsibility Principle:
  - Membuat `SettingsUi` untuk menyederhanakan pembuatan komponen UI seperti Spinner dan Section Label, menghindari duplikasi kode pada *SettingsActivity*.
  - Membuat `HiddenAppsDialog` untuk memisahkan logika pengelolaan *Hidden Apps* dari *SettingsActivity*.
  - Menyederhanakan `SettingsActivity` sehingga murni menangani *lifecycle*, merangkai *UI*, dan mengelola state *reset*.
  - Tidak ada perubahan pada perilaku fungsional atau antarmuka.
